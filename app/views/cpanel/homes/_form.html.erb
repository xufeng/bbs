<%= form_for(@cpanel_home) do |f| %>
  <% if @cpanel_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cpanel_home.errors.count, "error") %> prohibited this cpanel_home from being saved:</h2>

      <ul>
      <% @cpanel_home.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
